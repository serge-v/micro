filetype: patch

detect: 
    filename: "\\.(patch|diff)$"
    header: "^diff --git "

rules:
    - brightgreen: "^\\+.*"
    - green: "^\\+\\+\\+.*"
    - brightblue: "^ .*"
    - brightred: "^-.*"
    - red: "^---.*"
    - brightyellow: "^@@.*"
    - magenta: "^diff.*"
